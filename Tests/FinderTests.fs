namespace Tests
open System
open NUnit.Framework
open FsFinders 

module FinderTests = 
    
    [<Test>]
    let ``Should be able to convert any given list into a fuzzy item list``() =
        // TODO: Add the real tests here 
        Assert.True(false)
    
